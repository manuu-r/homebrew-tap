[package]
name = "gauge"
version = "0.7.0"
edition = "2021"
description = "Minimal Rust CLI for showing agent quota (Codex + Claude)."
license = "MIT"

[dependencies]
serde_json = "1"
winit = "0.30.13"
tray-icon = "0.24.2"
